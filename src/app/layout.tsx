import type {Metadata} from 'next';import './globals.css';import Header from '@/components/Header';import Sidebar from '@/components/Sidebar';import Providers from '@/components/Providers';
export const metadata:Metadata={title:'Modellbahn Katalog',description:'Visualisierung & Katalogisierung des Rollmaterials (H0 & N)'};
export default function RootLayout({children}:{children:React.ReactNode}){return(<html lang='de'><body><Providers><div className='layout'><Header/><div className='main'><Sidebar/>{children}</div></div></Providers></body></html>);}